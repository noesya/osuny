<%
root ||= 'data'
property ||= 'image'
title ||= ''
%>
<div v-if="!<%= property %>_id">
  <label  class="form-label"
          for="<%= property %>">
    <%= title %>
  </label>
  <input  class="form-control mb-2"
          type="file"
          accept="image/*"
          @change="onFileImageChange( $event, data, 'image_id' )"
          id="<%= root %>.<%= property %>_id">
</div>
<div v-if="data.image">
  <img :src="getImageUrl(<%= root %>.<%= property %>_id)"
        class="img-fluid mb-2"
        style="max-height: 80px"
        />
  <br>
  <a  class="btn btn-sm btn-danger mb-2"
      v-on:click="data.<%= property %>_id=null">
      <i class="fas fa-times"></i>
      <%= t 'admin.communication.blocks.components.image.remove' %>
  </a>
</div>
<label  class="form-label"
        for="<%= property %>_alt">
  <%= t 'admin.communication.blocks.components.image.alt.label' %>
</label>
<input id="<%= property %>_alt"
        type="text"
        class="form-control mb-2"
        v-model="<%= root %>.<%= property %>_alt"
        placeholder="<%= t 'admin.communication.blocks.components.image.alt.label' %>" />
<label  class="form-label" for="<%= property %>_credit">
  <%= t 'admin.communication.blocks.components.image.credit.label' %>
</label>
<input  id="<%= property %>_credit"
        type="text"
        class="form-control"
        v-model="<%= root %>.<%= property %>_credit"
        placeholder="<%= t 'admin.communication.blocks.components.image.credit.placeholder' %>" />
